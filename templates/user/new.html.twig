{% extends 'base.html.twig' %}

{% block title %}Nouvel utilisateur — DP Services{% endblock %}
{% block page_title %}Créer un utilisateur{% endblock %}

{% block body %}
    <div class="welcome">
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_row(form.firstname) }}
            {{ form_row(form.lastname) }}
            {{ form_row(form.email) }}
            {{ form_row(form.phone) }}
            {{ form_row(form.plainPassword) }}
            {{ form_row(form.roles) }}
            <button type="submit" class="btn btn--primary btn--lg mt-md">
                Créer
            </button>
            <a href="{{ path('app_users') }}" class="text-secondary text-secondary--spaced">Annuler</a>
        {{ form_end(form) }}
    </div>
{% endblock %}