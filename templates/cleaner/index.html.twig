<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Femme de ménage — DP Services</title>
    <link rel="stylesheet" href="{{ asset('css/dashboard.css') }}">
</head>
<body>

    <aside class="sidebar">
        <h2>DP Services</h2>
        <a href="{{ path('app_cleaner') }}" class="active">🧹 Mes missions</a>
        <a href="#">📅 Calendrier</a>
    </aside>

    <div class="main">
        <div class="topbar">
            <h1>Espace Femme de ménage</h1>
            <div class="user-info">
                <span>{{ app.user.firstname }} {{ app.user.lastname }}</span>
                <a href="{{ path('app_logout') }}" class="logout">Déconnexion</a>
            </div>
        </div>

        <div class="content">
            <div class="welcome">
                <h2>Vos missions du jour 🧹</h2>
                <p>Retrouvez ici toutes vos missions assignées.</p>
            </div>
        </div>
    </div>

</body>
</html>